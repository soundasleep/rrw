%ul
  %li.home
    = link_to 'World', controller: 'world'

  %li.stats
    = link_to current_player.name, controller: 'player'
    (level #{current_player.level})
    #{current_player.current_health}/#{current_player.total_health} health
