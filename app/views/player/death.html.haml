%h1 You are dead

%p
  - if current_player.killed_by
    You were killed by #{current_player.killed_by.name} (level #{current_player.killed_by.level}).
  - else
    You just died for some reason.

= form_tag("/player/reset", method: "post") do
  = submit_tag("Restart game")

= link_to "High scores", controller: 'scores'
